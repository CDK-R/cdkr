# isvalid.formula

Validate a cdkFormula.

## Usage

``` r
isvalid.formula(formula, rule = c("nitrogen", "RDBE"))
```

## Arguments

- formula:

  Required. A CDK Formula

- rule:

  Optional. Default `rule=c("nitrogen","RDBE")`
