# view.image.2d

view.image.2d

## Usage

``` r
view.image.2d(molecule, depictor = NULL)
```

## Arguments

- molecule:

  The molecule to display Should be a \`jobjRef\` representing an
  \`IAtomContainer\`

- depictor:

  Default `NULL`
