# do.aromaticity

detect aromaticity of an input compound

## Usage

``` r
do.aromaticity(mol)
```

## Arguments

- mol:

  The molecule to query. Should be a \`jobjRef\` representing an
  \`IAtomContainer\`
