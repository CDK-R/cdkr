# Export all names
exportPattern("^[[:alpha:]]+")

# Import all packages listed as Imports or Depends
importFrom("utils", "URLencode", "download.file", "read.csv")
import(
  XML,
  car,
  RCurl,
  RJSONIO,
  data.table
)
